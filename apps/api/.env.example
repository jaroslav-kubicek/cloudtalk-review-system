# Start Postgres locally:
#   docker run -d --name pg-reviews -p 5432:5432 -e POSTGRES_PASSWORD=dev -e POSTGRES_DB=reviews postgres:16

DATABASE_URL=postgres://postgres:dev@localhost:5432/reviews
JWT_SECRET=dev-secret-change-me
ADMIN_TOKEN=dev-admin-token
PORT=3000
